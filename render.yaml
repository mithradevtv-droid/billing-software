services:
  - type: web
    name: billing-system
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    disk:
      name: billing_db
      mountPath: /opt/render/project/src
      sizeGB: 1
