'use client'

export function PurchaseDetailsDialog({
  open,
  onOpenChange,
  purchase,
}: {
  open: boolean
  onOpenChange: (open: boolean) => void
  purchase: any
}) {
  if (!purchase) return null

  return null
}